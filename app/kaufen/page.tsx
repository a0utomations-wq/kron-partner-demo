'use client'

import Link from 'next/link'
import { ArrowLeft, ArrowRight, SlidersHorizontal } from 'lucide-react'
import { useMemo, useState } from 'react'
import { Button } from '@/components/ui/button'
import { ExposeModal, Footer, Header, PropertyCard, SectionEyebrow, properties } from '@/components/real-estate-shell'

export default function KaufenPage() { const [mode, setMode] = useState('Kaufen'); const [type, setType] = useState('Alle Objekttypen'); const [location, setLocation] = useState('Alle Regionen'); const [selected, setSelected] = useState<typeof properties[number] | null>(null); const shown = useMemo(() => properties.filter(p => (type === 'Alle Objekttypen' || p.type === type) && (location === 'Alle Regionen' || p.location.includes(location))), [type, location]); return <><Header /><main className="mx-auto max-w-7xl px-6 py-12 lg:px-10 lg:py-20"><Link href="/" className="inline-flex items-center gap-2 text-xs uppercase tracking-[0.18em] text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" /> Zurück zur Startseite</Link><div className="mt-12 flex flex-col justify-between gap-8 border-b border-border pb-10 md:flex-row md:items-end"><div><SectionEyebrow>Portfolio · {mode}</SectionEyebrow><h1 className="mt-4 font-serif text-5xl md:text-6xl">Immobilien finden.</h1><p className="mt-4 max-w-lg text-sm leading-6 text-muted-foreground">Ausgesuchte Objekte, sorgfältig geprüft und persönlich begleitet.</p></div><div className="flex border border-border p-1"><button onClick={() => setMode('Kaufen')} className={`px-5 py-2 text-sm ${mode === 'Kaufen' ? 'bg-foreground text-background' : ''}`}>Kaufen</button><button onClick={() => setMode('Mieten')} className={`px-5 py-2 text-sm ${mode === 'Mieten' ? 'bg-foreground text-background' : ''}`}>Mieten</button></div></div><div className="my-8 flex flex-wrap items-center gap-3 border-b border-border pb-8"><SlidersHorizontal className="mr-2 size-4" /><select value={type} onChange={e => setType(e.target.value)} className="border border-input bg-background px-4 py-3 text-sm"><option>Alle Objekttypen</option><option>Villa</option><option>Penthouse</option><option>Haus</option><option>Wohnung</option></select><select value={location} onChange={e => setLocation(e.target.value)} className="border border-input bg-background px-4 py-3 text-sm"><option>Alle Regionen</option><option>München</option><option>Düsseldorf</option><option>Hamburg</option><option>Berlin</option></select><span className="ml-auto text-sm text-muted-foreground">{shown.length} Objekte</span></div><div className="grid gap-x-8 gap-y-14 md:grid-cols-2 lg:grid-cols-3">{shown.map(p => <PropertyCard key={p.id} property={p} onOpen={() => setSelected(p)} />)}</div>{shown.length === 0 && <p className="py-20 text-center text-muted-foreground">Keine Objekte für diese Auswahl gefunden.</p>}</main><section className="border-y border-border bg-muted"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-5 px-6 py-14 md:flex-row md:items-center lg:px-10"><div><p className="font-serif text-2xl">Sie suchen etwas Besonderes?</p><p className="mt-2 text-sm text-muted-foreground">Wir finden, was nicht inseriert wird.</p></div><Button className="rounded-none" asChild><Link href="/#ueber-uns">Persönlich sprechen <ArrowRight data-icon="inline-end" /></Link></Button></div></section><Footer />{selected && <ExposeModal property={selected} close={() => setSelected(null)} />}</> }
